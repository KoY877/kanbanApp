import { DropManagerPipe } from './drop-manager-pipe';

describe('DropManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new DropManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
